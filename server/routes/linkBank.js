const express = require('express');
const router = express.Router();

// POST /api/link-bank
router.post('/', (req, res) => {
  // Placeholder: Link bank account
  res.json({ message: 'Link bank endpoint (not implemented)' });
});

module.exports = router;
